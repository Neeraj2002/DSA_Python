// Given an array arr[] of size n. The task is to find the first repearting element in the 
// array of integers, i.e., an element that occurs more than once and whose index of first 
// occurence is the smallest.
#include<iostream>
using namespace std;
// LOGIC:
// 1. Check if the element is repeating, we maintain an arr in idx[], which stores the first
// occurence of a particular element a[i].
//  i.  Initialise the idx[] with -1, and miniidx with INT_MAX.
//  ii. Keep updating idx[], while traversing the given array.
int main(){

}